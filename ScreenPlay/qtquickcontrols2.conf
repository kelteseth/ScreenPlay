; NOTE: this file only took effect after being embedded at the resource root
; (see qt_add_resources in CMakeLists.txt). Theme and accent are set in
; ScreenPlayMain.qml; Variant=Dense was removed because it never actually
; applied while the file was unread - re-adding it would densify the whole UI.
[Controls]
Style=Material

[Material]
Font\Family=Google Sans Flex
